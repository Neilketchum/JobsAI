const uploadController = require('./uploadController');
const testUploadController = require('./testUploadController');
const profileController = require('./profileController');

module.exports = {
  uploadController,
  testUploadController,
  profileController,
};
